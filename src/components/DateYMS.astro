---
import { format } from "date-fns";
import { ja } from "date-fns/locale";

interface Props {
  date: Date;
}

const { date } = Astro.props;
const text = format(date, "yyyy-MM-dd", { locale: ja });
---

{text}